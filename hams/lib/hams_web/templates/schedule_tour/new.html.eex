<h1>New Schedule tour</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.schedule_tour_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.schedule_tour_path(@conn, :index) %></span>
